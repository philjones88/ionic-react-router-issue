import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-router-blank-issue',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
